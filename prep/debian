#!/usr/bin/env bash
# Usage: lxc-setup debian

set +e

me="prep/debian"
echo ''
echo === Setup $me ===

# lxc-setup lib/common

dpkg-reconfigure locales
apt-get update && apt-get full-upgrade -y
apt-get autoremove -y

export DCODE="deb"

set -e

